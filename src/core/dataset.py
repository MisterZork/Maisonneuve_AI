import numpy
import os

#=============== Data collection ===============#


#=============== Data processing ===============#
def randomize_data():
    pass

def one_hot_decoding(data):
    """
    This function finds the maximum index and tries to create a list of values representing the one-hot encoding of the
    data.
    :param data: ndarray[float]: The dataset in which the maximum index is searched.
    :return: dict[int, str]: The dictionary containing the one-hot encoding of the data, and it's value.
    """
    pass

if __name__ == '__main__':
    create_dir()